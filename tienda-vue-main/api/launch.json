{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch API",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceFolder}/api/app.js",   // Ruta al archivo principal de tu aplicación
      "env": {
        "NODE_ENV": "development"  // Puedes establecer variables de entorno si es necesario
      }
    }
  ]
}
